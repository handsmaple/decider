{
  "name": "decider",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "deliberate": "npx tsx src/deliberation/demo.ts",
    "server": "npx tsx src/server/index.ts",
    "test": "npx tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.54.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0"
  }
}
